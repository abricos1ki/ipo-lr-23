# Procfile для Railway (лабораторная работа №23).
# Railway читает этот файл, чтобы знать, какой командой запускать приложение.
# gunicorn — WSGI-сервер для продакшна (вместо встроенного manage.py runserver).
web: gunicorn yoga_shop.wsgi:application
