# Учебный проект: интернет-магазин товаров для йоги

**Вариант: 22**
**Тема магазина: интернет-магазин товаров для йоги**
**Автор: Саплицкий Родион Александрович**

Репозиторий объединяет несколько лабораторных работ по Django,
выполняемых последовательно в рамках одного учебного проекта.

---

## Лабораторная работа №16. Создание и базовая настройка приложений Django

Базовая настройка проекта Django и приложения `shop`: подключение
приложения, настройка `settings.py` (язык, часовой пояс, БД),
URL-маршрутизация и три простые текстовые страницы.

### Страницы (text/plain)

| URL                | Описание                                              |
|---------------------|--------------------------------------------------------|
| `/`                  | Главная страница со ссылками на остальные страницы     |
| `/about-author/`     | Страница "Об авторе" — информация о том, кто выполнил лабораторную работу |
| `/about-shop/`       | Страница "О магазине" — информация о теме магазина (товары для йоги) |

---

## Лабораторная работа №17. Работа с Django ORM

Разработаны модели данных интернет-магазина, настроена админ-панель,
реализованы ORM-запросы (фильтрация, сортировка, поиск, пагинация),
шаблоны для отображения списка товаров и страницы детализации.

### Модели (shop/models.py)

- **Category** ("Категория товара") — `name`, `description`.
- **Manufacturer** ("Производитель") — `name`, `country`, `description`.
- **Product** ("Товар") — `name`, `description`, `photo`, `price`,
  `stock_quantity`, `category` (FK), `manufacturer` (FK).
  Цена и количество на складе не могут быть отрицательными
  (`MinValueValidator` + проверка в `clean()`).
- **Cart** ("Корзина") — `user` (OneToOne к `User`), `created_at`.
  Метод `total_cost()` (он же `общая_стоимость`) считает сумму по всем
  элементам корзины.
- **CartItem** ("Элемент корзины") — `cart` (FK), `product` (FK),
  `quantity`. Метод `item_cost()` (он же `стоимость_элемента`) возвращает
  `товар.цена * количество`. В `clean()` проверяется, что количество не
  превышает остаток на складе.

Все связи `ForeignKey`/`OneToOneField` используют `on_delete=models.CASCADE`.

### Админ-панель

Все 5 моделей зарегистрированы в `shop/admin.py` со списками,
фильтрами и поиском. Элементы корзины отображаются inline внутри
карточки корзины.

### Страницы каталога

| URL                       | Описание                                                       |
|-----------------------------|------------------------------------------------------------------|
| `/products/`                | Список товаров: фильтр по категории/производителю, поиск (`q`), сортировка (`sort`), пагинация по 10 товаров на странице |
| `/products/<id>/`           | Страница детализации товара (404, если товар не найден)         |

GET-параметры `/products/`:
- `category=<id>` — фильтр по категории
- `manufacturer=<id>` — фильтр по производителю
- `q=<текст>` — поиск по названию или описанию (через `Q`-объекты)
- `sort=price_asc | price_desc | name` — сортировка
- `page=<номер>` — пагинация

### Наполнение базы данных

Команда `python manage.py seed_data` заполняет БД тестовыми данными:

- 5 производителей
- 10 категорий
- 34 товара
- 5 пользователей (`anna`, `ivan`, `olga`, `dmitry`, `maria`,
  пароль `password123`), у каждого создана корзина с 2 позициями.

---

## Структура проекта

```
.
├── manage.py
├── requirements.txt
├── README.md
├── .gitignore
├── yoga_shop/                 # конфигурация проекта
│   ├── settings.py
│   ├── urls.py
│   ├── wsgi.py
│   └── asgi.py
└── shop/                      # приложение магазина
    ├── views.py
    ├── urls.py
    ├── models.py
    ├── admin.py
    ├── templates/shop/        # HTML-шаблоны (лр17)
    │   ├── base.html
    │   ├── product_list.html
    │   └── product_detail.html
    ├── management/commands/
    │   └── seed_data.py       # наполнение БД тестовыми данными
    └── migrations/
```

## Настройки проекта (settings.py)

- **База данных:** SQLite (`django.db.backends.sqlite3`), файл
  `db.sqlite3` создаётся автоматически после миграций.
- **Часовой пояс:** `Europe/Minsk`
- **Язык интерфейса:** `ru` (русский)
- **MEDIA_URL / MEDIA_ROOT** настроены для загрузки фото товаров.
- **Приложение `shop`** подключено в `INSTALLED_APPS`.

## Установка и запуск

1. Клонировать репозиторий:
   ```bash
   git clone <ссылка на репозиторий>
   cd <папка проекта>
   ```

2. Создать и активировать виртуальное окружение:

   **Windows:**
   ```bash
   python -m venv venv
   venv\Scripts\activate
   ```

   **Linux / macOS:**
   ```bash
   python3 -m venv venv
   source venv/bin/activate
   ```

3. Установить зависимости:
   ```bash
   pip install -r requirements.txt
   ```

4. Применить миграции:
   ```bash
   python manage.py migrate
   ```

5. (Опционально) Заполнить базу тестовыми данными:
   ```bash
   python manage.py seed_data
   ```

6. (Опционально) Создать суперпользователя для входа в админ-панель:
   ```bash
   python manage.py createsuperuser
   ```

7. Запустить сервер разработки:
   ```bash
   python manage.py runserver
   ```

8. Открыть в браузере:
   - Главная страница: [http://127.0.0.1:8000/](http://127.0.0.1:8000/)
   - Каталог товаров: [http://127.0.0.1:8000/products/](http://127.0.0.1:8000/products/)
   - Админ-панель: [http://127.0.0.1:8000/admin/](http://127.0.0.1:8000/admin/)

## Используемые технологии

- Python 3
- Django 5.1
- Pillow (для работы с изображениями товаров)
- SQLite

## Автор

Саплицкий Родион Александрович
Вариант 22 (тема: интернет-магазин товаров для йоги).
